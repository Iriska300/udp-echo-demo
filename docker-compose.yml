services:
  echo-server:
    build: .
    container_name: udp-echo-server
    ports:
      - "8080:8080/udp"
    networks:
      - vran-network
    mem_limit: 128m
    cpus: '0.5'
    restart: unless-stopped

networks:
  vran-network:
    driver: bridge
