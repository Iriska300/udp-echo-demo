apiVersion: v1
kind: Service
metadata:
  name: udp-echo-service
  namespace: default
spec:
  selector:
    app: udp-echo-server
  ports:
    - port: 8080
      targetPort: 8080
      protocol: UDP
  type: ClusterIP
